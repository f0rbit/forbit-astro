---
import { get_blog_posts, type AppLocals } from '../../utils'
import BlogCard from './BlogCard.astro'

const posts = (await get_blog_posts(Astro.locals as AppLocals)).slice(0, 3)
---

<div class="grid">
    {posts.map((post: any) => <BlogCard post={post} />)}
</div>
